import 'package:validar_brasileiro/validar_brasileiro.dart';

void main() {
  // Validators land in the next change. This example keeps the package
  // publishable and proves the public import resolves.
  final Type namespace = ValidarBrasileiro;
  assert(namespace == ValidarBrasileiro);
}
